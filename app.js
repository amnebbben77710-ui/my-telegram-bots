async function api(url, opts={}){const r=await fetch(url,opts);let j={};try{j=await r.json()}catch{}if(!r.ok)throw Error(j.error||j.message||"حدث خطأ");return j}
function esc(s){return String(s).replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[c]))}
async function load(){const box=document.getElementById("bots");try{const bs=await api("/api/bots");if(!bs.length){box.innerHTML='<div class="card">لا توجد بوتات بعد.</div>';return}box.innerHTML=bs.map(b=>`<div class="bot"><div><h3>${esc(b.name)}</h3><div class="meta">${esc(b.filename)}</div><div class="${b.running?'running':'stopped'}">${b.running?'● يعمل':'● متوقف'}</div></div><div class="actions"><button class="secondary" onclick="action('${b.id}','start')">تشغيل</button><button class="secondary" onclick="action('${b.id}','restart')">إعادة تشغيل</button><button class="secondary" onclick="logs('${b.id}')">Logs</button><button class="secondary" onclick="action('${b.id}','stop')">إيقاف</button><button class="danger" onclick="delBot('${b.id}')">حذف</button></div></div>`).join("")}catch(e){box.innerHTML='<div class="card err">'+esc(e.message)+'</div>'}}
async function action(id,a){try{await api(`/api/bots/${id}/${a}`,{method:"POST"});load()}catch(e){alert(e.message)}}
async function upload(){const token=document.getElementById("token").value.trim(),f=document.getElementById("file").files[0],m=document.getElementById("msg");m.className="";if(!token||!f){m.textContent="أدخل التوكن واختر الملف.";m.className="err";return}const fd=new FormData();fd.append("token",token);fd.append("file",f);m.textContent="جارٍ الرفع والتشغيل...";try{await api("/api/bots",{method:"POST",body:fd});document.getElementById("token").value="";document.getElementById("file").value="";m.textContent="تم تشغيل البوت.";m.className="ok";load()}catch(e){m.textContent=e.message;m.className="err"}}
async function logs(id){try{const j=await api(`/api/bots/${id}/logs`);document.getElementById("logtext").textContent=j.log||"لا توجد Logs بعد.";document.getElementById("logs").showModal()}catch(e){alert(e.message)}}
async function delBot(id){if(!confirm("حذف البوت وملفاته؟"))return;try{await api(`/api/bots/${id}`,{method:"DELETE"});load()}catch(e){alert(e.message)}}
load();setInterval(load,5000);
